# [901. Online Stock Span](https://leetcode.com/problems/online-stock-span/description/){target="_blank"}

## Description

Design an algorithm that collects daily price quotes for some stock and returns the span of that stock's price for the current day.

The span of the stock's price in one day is the maximum number of consecutive days (starting from that day and going backward) for which the stock price was less than or equal to the price of that day.

## Examples

=== "Input"
    > ["StockSpanner", "next", "next", "next", "next", "next", "next", "next"]
    >
    > [[], [100], [80], [60], [70], [60], [75], [85]]

=== "Output"
    > [null, 1, 1, 1, 2, 1, 4, 6]

=== "Explanation"
    > StockSpanner stockSpanner = new StockSpanner();
    >
    > stockSpanner.next(100); // return 1
    >
    > stockSpanner.next(80);  // return 1
    > 
    > stockSpanner.next(60);  // return 1
    >
    > stockSpanner.next(70);  // return 2
    >
    > stockSpanner.next(60);  // return 1
    >
    > stockSpanner.next(75);  // return 4, because the last 4 prices (including today's price of 75) were less than or equal to today's price.
    >
    > stockSpanner.next(85);  // return 6

## Stub

```kotlin
class StockSpanner() {

  fun next(price: Int): Int {
    // Fill here
  }

}
```

## Solution

??? "Expand"

    It is exactly like any other NGE problem, except this time we have to maintain the array since it's an ongoing algo.

    ```kotlin
    class StockSpanner() {

      data class Pair(val day: Int, val price: Int) // (1)

      private var day = 0
      private val stack = ArrayDeque<Pair>()

      fun next(price: Int): Int {
        while (stack.isNotEmpty() && stack.last().price <= price) {
          stack.removeLast()
        }
        val span = if (stack.isEmpty()) day + 1 else day - stack.last().day
        stack.addLast(Pair(day++, price))
        return span
      }

    }
    ```

    1. alternative to having to save each and all prices in an array.

## Unit tests

```kotlin
import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test

class Solution {

  val next = StockSpanner()::next

  @Test
  fun `example 1`() {
    assertThat(next(100)).isEqualTo(1)
    assertThat(next(80)).isEqualTo(1)
    assertThat(next(60)).isEqualTo(1)
    assertThat(next(70)).isEqualTo(2)
    assertThat(next(60)).isEqualTo(1)
    assertThat(next(75)).isEqualTo(4)
    assertThat(next(85)).isEqualTo(6)
  }

}
```